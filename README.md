# ai-qa-talk

Static HTML slide deck for the talk **"Как ИИ усиливает QA"**.

## Run locally

```bash
python3 -m http.server 5173
```

Open `http://localhost:5173/`.

## Print to PDF

Open `/?print=1` and use the browser Print dialog.
